import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#bac3b8',
    },
    secondary: {
      main: '#d7ccb8',
    },
  },
});

export default theme;
